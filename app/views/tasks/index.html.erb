<!DOCTYPE html>
<html>
<head>
  <title>AntiProcrastination</title>
</head>
<body>

<div class="center"><h2>  Today is <%= Time.now.strftime("%A, %B %d") %> </h2> </div>

<%= link_to 'Create Task', new_task_path%>
 
  <div class="sorts">
     <%= sortable "title"%> 
     <%= sortable "due"%> 
     <%= sortable "priority"%> 
  </div>


  <% @tasks.each do |t| %>
      <% if t.priority == 1%><div class="square1"><% end %><% if t.priority == 2%><div class="square2"><%end %><% if t.priority == 3%><div class="square3"><% end %> 
      <div class="title"><%= t.title %></div>
      <div class="description"> <%= t.description %> </div>
      Priority: <% if t.priority == 1%>High<% end %><% if t.priority == 2%>Medium<%end %><% if t.priority == 3%>Low<% end %> <br>
      <%= t.due.strftime("%I:%M on %A %m/%d") %> <br>
      <%= time_ago_in_words(Time.now) %>
      <%= t.created_at %>
      <%= link_to 'Show', task_path(t) %>
      <%= link_to 'Edit', edit_task_path(t) %>
      <%= link_to 'Destroy', task_path(t), method: :delete, data: { confirm: 'Are you sure?' } %>
     </div>
  <% end %>  

</body>

